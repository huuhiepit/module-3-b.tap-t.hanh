create database demo;

use demo;

create table Students (
	id int,
    name varchar(200),
    age int,
    country varchar(50)
);

create table Classs (
	id int,
    name varchar(50)
);

create table Teachers (
	id int,
    name varchar(50),
    age int,
    country varchar(100)
);